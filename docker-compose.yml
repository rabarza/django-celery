services:
  web:
    container_name: django_web
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./src:/usr/src/app
    ports:
      - "8002:8000"
    env_file:
      - .env